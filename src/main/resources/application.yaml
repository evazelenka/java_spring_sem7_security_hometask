spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mysecurity_db
    username: myuser
    password: 8
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: false
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect